# Bro Code fixtures

Drop `*.bundle.json` captures here (CAPTURE FIXTURE / `dart run tool/pull_bro_code_fixture.dart`).

Bundles are gitignored by default. Commit a specific fixture only when you intend it as a regression case (force-add).
